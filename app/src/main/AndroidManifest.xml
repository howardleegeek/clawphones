<manifest package="com.clawphones" xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="ClawPhones"
        android:icon="@mipmap/ic_launcher">
        <!-- Firebase Analytics configuration placeholder -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />
    </application>
</manifest>
